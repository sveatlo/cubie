# Talos
talos/secrets.yaml
talos/configs/

# Kubernetes credentials
kubeconfig
*.kubeconfig
kubeconfig.yaml

# age keys
keys.txt
*.agekey

# SOPS decrypted files (never commit plaintext secrets)
*.sops.decrypted.*

# Render-time vars: only the encrypted .yaml.sops is committed, never plaintext
vars/*.yaml

# Editor / OS
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Temporary
tmp/
*.tmp
.worktrees/

docs/plans/
kubernetes/**/charts/
