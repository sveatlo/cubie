---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: babybuddy-db
  namespace: babybuddy
spec:
  instances: 1

  postgresql:
    parameters:
      shared_buffers: "256MB"
      effective_cache_size: "512MB"

  storage:
    size: 5Gi
    storageClass: nfs

  bootstrap:
    initdb:
      database: babybuddy
      owner: babybuddy
      encoding: UTF8
      localeCType: C
      localeCollate: C
