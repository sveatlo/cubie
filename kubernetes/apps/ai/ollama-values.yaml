---
# Standalone Ollama configuration

# Pin to talos-891-uxr node
nodeSelector:
  kubernetes.io/hostname: talos-891-uxr

ollama:
  gpu:
    enabled: false # CPU-only (AMD iGPU unreliable for ROCm)
  models:
    pull:
      - deepseek-r1:8b

# Persistent storage for models (survives pod restarts)
persistentVolume:
  enabled: true
  size: 30Gi
  storageClass: nfs

# Extra env vars for better CPU inference
extraEnv:
  - name: OLLAMA_KEEP_ALIVE
    value: "24h"
  - name: OLLAMA_NUM_PARALLEL
    value: "1"
  - name: OLLAMA_MAX_LOADED_MODELS
    value: "2"

service:
  type: ClusterIP
  port: 11434

ingress:
  enabled: false # Ollama is internal-only, accessed via cluster DNS

resources:
  requests:
    memory: "8Gi"
