apiVersion: traefik.io/v1alpha1
kind: TLSOption
metadata:
  name: default
  namespace: traefik
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  minVersion: VersionTLS12
  sniStrict: false
