---
# L2Advertisement binds the IP pool to L2 (ARP/NDP) announcements.
# No BGP peer configuration needed for a flat homelab LAN.
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: lan-l2adv
  namespace: metallb-system
spec:
  ipAddressPools:
    - lan-pool
