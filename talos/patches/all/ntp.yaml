---
# NTP time sync configuration.
# EDIT: replace with your preferred NTP servers (local LAN server recommended).
machine:
  time:
    disabled: false
    servers:
      - time.cloudflare.com
      - pool.ntp.org
